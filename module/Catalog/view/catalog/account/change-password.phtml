<?php
$title = "Change Your Password";
$this->headTitle($title);
$form = $this->form;

?>
<?php
if($this->flashMessenger()->hasCurrentErrorMessages()){
    foreach($this->flashMessenger()->getCurrentErrorMessages() as $msg){
        echo '<div class="alert alert-danger" role="alert">'.$msg.'</div>';
    }
}

?>
<?php
if($this->flashMessenger()->hasCurrentSuccessMessages()){
    foreach($this->flashMessenger()->getCurrentSuccessMessages() as $msg){
        echo '<div class="alert alert-success" role="alert">'.$msg.'</div>';
    }

}
$this->flashMessenger()->clearCurrentMessagesFromContainer();
?>
<div class="row">
    <div class="ares-box">
        <h3><?php echo $title; ?></h3>
        <hr>
        <p>Fields marked with * are required.</p>
        <hr>
        <?php echo $this->form()->openTag($form); ?>
        <div class="col-sm-6">
            <?php
            echo $this->formRow($form->get('password'), null, false);
            if ($this->formElementerrors($form->get('password'))) {
                echo '<div class="alert alert-danger" role="alert">' . $this->formElementerrors($form->get('password')) . '</div>';
            }
            ?>

        </div>
        <div class="col-sm-6">
            <?php
            echo $this->formRow($form->get('cpassword'), null, false);
            if ($this->formElementerrors($form->get('cpassword'))) {
                echo '<div class="alert alert-danger" role="alert">' . $this->formElementerrors($form->get('cpassword')) . '</div>';
            }
            ?>

        </div>


        <div class="col-sm-12">
            <?php
            echo $this->formRow($form->get('currentpassword'), null, false);
            if ($this->formElementerrors($form->get('currentpassword'))) {
                echo '<div class="alert alert-danger" role="alert">' . $this->formElementerrors($form->get('currentpassword')) . '</div>';
            }
            ?>
        </div>


        <div class="col-sm-12">
            <?php
            echo $this->formRow($form->get('csrf'), null, false);
            if ($this->formElementerrors($form->get('csrf'))) {
                echo '<div class="alert alert-danger" role="alert">' . $this->formElementerrors($form->get('csrf')) . '</div>';
            }
            ?>
        </div>


        <div class="col-sm-12" style="text-align: right;">
            <br>
            <input type="button" class="btn btn-default" value="Clear" onclick="clearInputs(this);" />
            <?php
            echo $this->formRow($form->get('submit'), null, false);
            ?>
        </div>
        <?php echo $this->form()->closeTag(); ?>
    </div>

</div>
<div style="clear:both"></div>
<style>
    label span, label input, label{
        display: block;
        width: 100% ;
    }
    label span{
        font-weight: normal;
    }

    .newsletter label span, .newsletter label input, .newsletter label{
        display: inline;
        width: auto;

    }
    .newsletter label input[type=checkbox]{
        float: left;
        margin-top: -7px;
        margin-right: 5px;
    }

</style>
